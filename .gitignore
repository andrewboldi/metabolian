/target

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key

# Node / web build
node_modules/
dist/
web/public/graph/
*.tsbuildinfo
.DS_Store
npm-debug.log*

# Ingest source tables — large, reproducible via tools/ingest/fetch.sh.
# Everything DERIVED from them (modules, layouts, sheets.json) is committed.
data/ingest/*.tsv
data/ingest/*.dat
